# 安全说明（SECURITY）

## 数据与隐私

- 本插件所有媒体解析（图片描述注入、语音转文本、音乐/视频解析、频谱与深度分析）
  均在 AstrBot 所在主机本地完成，不会将媒体内容上传至任何第三方服务；
- 对外网络请求仅包括：
  - 消息中出现的链接（网易云音乐、B 站等）的解析与下载；
  - 「环境配置」中由用户主动触发的依赖安装（pip 镜像源）。
- 解析过程中产生的临时文件存放于插件数据目录（可配置），并在任务结束或定时清理时删除；
  如开启了「保留临时文件」选项，请自行注意磁盘占用与敏感内容管理。

## 权限边界

- 本插件不修改 AstrBot 源码文件；图片增强通过运行时包装（monkey patch）实现，
  插件停止/重载时自动还原；
- 插件页面运行于 AstrBot 的受限 iframe 中，所有请求经 Dashboard 转发与鉴权。

## 依赖安全

- 必选依赖：ffmpeg/ffprobe、yt-dlp（系统级工具）；
- 可选依赖：librosa、scipy、soundfile（用于音频深度分析，由用户在页面确认后安装）；
- 建议使用官方或可信镜像源安装依赖，并保持工具为较新版本。

## 报告问题

如发现安全问题，请通过仓库 Issues 反馈（请勿公开敏感信息）：
<https://github.com/baizi51676-source/astrbot_plugin_multimodal_enhance/issues>
